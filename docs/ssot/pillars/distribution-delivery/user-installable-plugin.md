# 分发完整性：其他用户可正式安装

**date**: 2026-08-15
**pipeline**: ops / delivery / acceptance
**range**: 全仓分发面（打包、正式 omp plugin 安装、发布、CI）

## 用户支柱 (2026-08-15, 原文)

> 这说明我们之前根本就没有对于打包发布和正式的omp plugin安装流程进行完整的调查搜索了解和在项目中的搭建. 对于这个项目, 这块内容需要被完整, 才能够让其他用户正常使用. 你们应当首先进行充分调查, 落盘research以及相关默认最佳实践, 得出打包发布部署方案, 然后完整设计实现验证, 直到项目状态达到完美, 所有相关代码和配置等等内容根据默认最佳实践闭环.

（范围注：本支柱要求的是分发完整性，使仓库外的用户能按官方 omp plugin 流程安装并使用；不是设计草稿。）

## 用户澄清 (2026-08-15, 原文，第二条)

> 不是, 那么为什么我们要维护这样一个marketplace呢? 而且你确定npm包和project scope有关系吗? 
> 没有key的阻塞点在我这里, 我后面打算直接给的啊, 这根本就不是问题.
> 我建议你重新全量进行你的任务.

（范围注：本条是对 2026-08-15 第一次分发实现的否决。作者要求重做全部分发调查与实现；不要为了发插件而维护一个 marketplace；不要把「npm 与 project scope」绑成选路理由；不要把尚未提供的 npm token 当成通道否决条件。）

## 用户澄清 (2026-08-15, 原文，第三条)

> C:\Users\15480\Desktop\AIWorkshop\repos\omp-qol\tmp_token.txt token已经创建好了, 实装之后自己删除.
> 然后, 你们要明白, ci workflow是他吗全开源社区都在用的, 最应该存在最佳实践的技术栈. 我不认为给出的问题应该向上述你们给出的这么琐碎. 我建议你们还是首先对于这个领域问题进行完整搜索调查研究, 给出默认最佳实践, 在用这个实践对照着目前项目现状, 进行后续工作. 这个任务不应该有这么难和复杂.

（范围注：token 文件只用于写入 GitHub 仓库密钥，实装后删除；CI/npm publish 先对照开源社区默认实践，只补默认缺口。）

## 用户澄清 (2026-08-15, 原文，第四条)

> 已经上架正确安装方式, 就代表我们之后测试工作区也要使用这种方式.
> 你自己完整查看当前项目中的各种测试和工作区配置, 完整全量对于插件安装方式进行维护更新.
>
> 需要注意的是, 当前OMP不论是全局还是这个测试工作区, 都有任务在跑, 所以你必须要注意不能够扰乱当前进程, 仅修改相关文件, 干净严谨安全地执行这部分工作.

（范围注：上架后的默认测试/验收安装是 `omp plugin install omp-qol-plugin`；隔离根必须设 `PI_CONFIG_DIR` 或隔离 HOME，不得写作者 `~/.omp`，也不得热换 live `test-workspace/.omp`。活会话结束前不重装 live 根。）
