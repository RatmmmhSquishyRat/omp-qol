# I1: Ingest 2026-08-09 — inherit / re-verify / overturn

**Track:** I1 (`research/00-index.md`)  
**Date:** 2026-08-16  
**Scope:** Claim-by-claim ingest of the frozen 2026-08-09 ICM foundation package. No product code. Handoff files were read only.

This file classifies architecture-changing claims so the 2026-08-09 package is **prior, not lock** (`DECISIONS.md`). Host facts that H1–H5 already confirmed on 17.3.4 are marked **inherit as working law** with the closer that closed them. Product choices that 2026-08-09 left open stay open here.

---

## Baselines

| | 2026-08-09 handoff | This program (2026-08-16) |
|---|---|---|
| Upstream | `can1357/oh-my-pi` @ `45e12e5bb758198a920c6070e7e64cb33b21beac` | `docs/ref_repos/oh-my-pi-main` @ `de6b7974a0` |
| Coding agent | **17.2.12** | **17.3.4** |
| Comparison | OpenCode DCP @ `85b6f5c`; Prime Agent then-current | same DCP lock + Pi-DCP ports + ACM + btw (see `00-index.md`) |
| Author scope | Full OMP-QOL (Goal / Plan / Advisor / DCP / Pin / PrimeStyle) | **compress + pin + pin tree only** |

---

## Disposition vocabulary

| Label | Meaning |
|---|---|
| **inherit as working law** | Keep as program law or working hypothesis. Do not silently rewrite. Includes host facts already re-verified on 17.3.4 with no overturn. |
| **must re-verify on 17.3.4** | Host- or provider-dependent. Mechanism may be confirmed; measurement, adversarial cases, or economics still open. |
| **already overturned** | Original-proposal assumption that 2026-08-09 disproved. Do not revive. The *replacement finding* is a separate inherit/re-verify row. |
| **out of v1 scope** | Advisor / PrimeStyle / Goal / Plan / Vibe / managed-harness. Adjacent; record only. |

**Closer** is the workspace research file that supplies (or already supplied) the evidence. Product locks go to `../DECISIONS.md`, not this ingest.

---

## How to read

- **Claim** is the 2026-08-09 statement (finding or original assumption).
- **2026-08-09 status** uses the handoff register: CLOSED / STRONG / OPEN / DEFER / DISPROVEN / REJECTED.
- ICM completeness: compress, pin, tree, provenance, cache, headroom. Advisor and PrimeStyle are in §9 only.
- H1–H5 landed in parallel with this ingest. Notes cite their verdicts; they do **not** freeze C vs D, public `@message` syntax, or sealed-expand UX.

---

## 0. Original assumptions already overturned (do not revive)

These are the *false* original-proposal claims. The replacement findings are in later sections.

| ID | Claim (original, false) | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| O1 | 17.2.4 / 17.2.8 behavior is current enough to freeze lifecycle | DISPROVEN | already overturned | [`host-delta-17.3.md`](./host-delta-17.3.md) | Now even 17.3.4. H5: core pressure/cache/context-event files mostly unchanged; orchestration and new seams appeared. |
| O2 | If QOL compresses in the `context` hook, native auto-compaction will respect the reduced headroom | DISPROVEN | already overturned | [`host-compaction.md`](./host-compaction.md) | H1 confirmed still false: `compactionContextTokens = max(provider, stored)`. Q1 closed at E2. |
| O3 | Overlay state requires rewriting session messages or a sidecar DB | DISPROVEN | already overturned | [`host-context-event.md`](./host-context-event.md) | Replacement: `pi.appendEntry`. H2 confirmed. Pi-vault already uses this; OpenCode DCP still uses a sidecar — do not copy that. |
| O4 | Public `context` event exposes `SessionEntry` IDs | DISPROVEN | already overturned | [`host-addressing.md`](./host-addressing.md) | H2+H4+H5: still copies only. Private assistant Symbol is not a public seam. |
| O5 | Content-matching a rebuilt native context is a robust permanent address contract | DISPROVEN | already overturned | [`host-addressing.md`](./host-addressing.md), [`host-context-event.md`](./host-context-event.md) | Serial handlers; plugin not first; `structuredClone` drops non-enumerable maps. |
| O6 | Expand requires copying raw payload into the compression block | DISPROVEN (while unsealed) | already overturned | [`host-addressing.md`](./host-addressing.md) | Journal remains SoT until native seal. After seal, expand is a different operation (HD12). |
| O7 | Native compaction must always re-summarize already-authored DCP history | DISPROVEN | already overturned | [`host-compaction.md`](./host-compaction.md) | Custom `CompactionResult` from `session_before_compact` skips LLM/snapcompact. H1 confirmed. |
| O8 | Arbitrary semantic range deletion is provider-safe | DISPROVEN | already overturned | [`dcp-opencode.md`](./dcp-opencode.md), [`pi-dcp.md`](./pi-dcp.md) | Replacement: protocol-safe closure (PT1). |
| O9 | Pin may replay the original provider message type anywhere, including a raw standalone `toolResult` | DISPROVEN | already overturned | [`opencode-acm.md`](./opencode-acm.md), [`pin-ecosystem.md`](./pin-ecosystem.md) | Replacement: provider-neutral renderer (PN1). |
| O10 | Pin affecting compaction requires forking the native compactor | DISPROVEN | already overturned | [`host-compaction.md`](./host-compaction.md) | Use `session.compacting` and/or custom seal. `session.compacting` cannot replace `messagesToSummarize`. |
| O11 | Any in-place history rewrite loses the entire prompt cache | DISPROVEN | already overturned | [`host-cache.md`](./host-cache.md) | Replacement: first-divergence + suffix (CA1). H3: compaction *shrink* still full-clears the append-only log — that is a refinement, not a revival of this claim. |
| O12 | PinStateTree is the pin core / should ship as pin v1 | DEFER / rejected for v1 | already overturned | [`pin-ecosystem.md`](./pin-ecosystem.md), [`opencode-acm.md`](./opencode-acm.md) | Tree is policy over PinSpecs (TR1). Still in this program as Phase 6, not pin storage. |
| O13 | `ctx.reload()` is a generic plugin/config refresh | DISPROVEN | out of v1 scope | [`plugin-seams.md`](./plugin-seams.md) | Adjacent. It reopens/switches the session file. Do not reuse as an ICM “refresh overlay” story. |
| O14 | New WATCHDOG edits require restart; `/advisor off/on` rediscovers config | DISPROVEN | out of v1 scope | [`plugin-seams.md`](./plugin-seams.md) | Adjacent. QOL-004 already drives save → discover → `applyAdvisorConfigs`. |
| O15 | Unrestricted file edit is the PrimeStyle unlock; live extension self-edit is a v1 feature | DISPROVEN / REJECTED | out of v1 scope | — | Adjacent PrimeStyle. Restart-class for running extension source. |
| O16 | AGENTS/context file edits and SYSTEM/APPEND file edits are the same live-apply | DISPROVEN | out of v1 scope | [`host-delta-17.3.md`](./host-delta-17.3.md) | Adjacent. H5 did not re-diff `refreshBaseSystemPrompt`; no signal of removal. |

---

## 1. Headroom / native lifecycle

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| HD1 | Native pressure = `max(provider-reported usage, stored-conversation estimate)`, explicitly to defeat on-wire compression extensions | CLOSED (opposite assumption DISPROVEN) | inherit as working law | [`host-compaction.md`](./host-compaction.md) | H1 E2 confirmed. `compaction.ts` unchanged 17.2.12→17.3.4 (H5). Closes Q1: floor still present. |
| HD2 | A 150k raw / 20k projected session can still trigger native auto-compaction | STRONG | inherit as working law | [`host-compaction.md`](./host-compaction.md) | H1: `max(20k, 150k) = 150k` vs `resolveThresholdTokens`. Stored estimate counts active messages, not wire payload. |
| HD3 | Reversible model-request projection and native headroom accounting are different layers | CLOSED | inherit as working law | [`host-compaction.md`](./host-compaction.md) | Invariant 12. Overlay can shrink the provider prompt without convincing OMP the journal is small. |
| HD4 | `session_before_compact` can cancel **or** return a custom `CompactionResult` | CLOSED | inherit as working law | [`host-compaction.md`](./host-compaction.md) | H1 confirmed. Last registered handler wins (no merge). Do not register a competing seal handler. |
| HD5 | A custom `CompactionResult` reuses mature DCP summaries and skips a second LLM/snapcompact summarize | CLOSED | inherit as working law | [`host-compaction.md`](./host-compaction.md) | H1: `fromHook` skips `compact()`. Host still computes the cut *before* the hook; plugin must respect `firstKeptEntryId` / valid branch IDs. |
| HD6 | `session.compacting` can add context / prompt / `preserveData` but does **not** replace `messagesToSummarize` | CLOSED | inherit as working law | [`host-compaction.md`](./host-compaction.md) | H1 confirmed. Skipped entirely when a custom `compaction` is returned. Pins can guide ordinary native summarize this way; they cannot avoid a second summarize without HD5. |
| HD7 | Native compaction is append-only at the journal (`CompactionEntry` + `firstKeptEntryId`); old entries remain on disk | CLOSED | inherit as working law | [`host-compaction.md`](./host-compaction.md) | H1 confirmed. LLM context is sliced at the latest compaction; transcript mode keeps superseded markers. |
| HD8 | Architecture **A** (overlay only; native compaction stays authoritative) is a baseline/reference arm, not the preferred final architecture | STRONG | inherit as working law | [`host-compaction.md`](./host-compaction.md), [`../DECISIONS.md`](../DECISIONS.md) | Still the right A/B test arm. Does not own headroom while HD1 holds. |
| HD9 | Architecture **B** (cancel threshold/idle compaction when projected usage is safe) is a diagnostic experiment only; never the default | STRONG | inherit as working law | [`host-compaction.md`](./host-compaction.md) | Cancel exists. Floor retriggers every turn. Never cancel overflow / incomplete / manual. Conflicts with host safety accounting. |
| HD10 | Architecture **C** (overlay + native `CompactionEntry` seal) is the recommended plugin-first v1 if exact indefinite expand is not mandatory | STRONG lean, not frozen | inherit as working law | [`../DECISIONS.md`](../DECISIONS.md) | Inherit the *lean and the option set*, not a freeze. H1: plugin can seal, cannot own reserve/threshold or the cut algorithm. H5: C not overturned; D not shipped. Q3 still open. Pillar “expand any summary” leans D. |
| HD11 | Architecture **D** (trusted core projection-ownership) is the preferred long-term path if exact reversible lifecycle is a hard product requirement | STRONG option | inherit as working law | [`../DECISIONS.md`](../DECISIONS.md) | Must not weaken overflow safety; provider occupancy is a lower bound. No 17.3.4 seam appeared (H5). |
| HD12 | After a C seal, `expand` is not “re-enable raw messages in ordinary `buildSessionContext` output”. It is `rehydrate` / `branch` / `exactExpansionAvailable: false`, and must be visible to the model | OPEN (Q4) | inherit as working law | [`../DECISIONS.md`](../DECISIONS.md) | Inherit the *distinction*. Exact UX not frozen. H4: post-seal native projection starts at `firstKeptEntryId`. |
| HD13 | Compression blocks must be independent of materialization (`active-overlay` / `disabled` / `shadowed` / `sealed-native-compaction` / `invalid-source`) so C can migrate to D | STRONG | inherit as working law | [`host-compaction.md`](./host-compaction.md) | Do not bake `CompactionEntry` into the block model. |
| HD14 | Never cancel overflow / incomplete / manual recovery | STRONG | inherit as working law | [`host-compaction.md`](./host-compaction.md) | H1 reason table: overflow retries; incomplete is length-stop; manual is `/compact`. |
| HD15 | Default native strategy on current host is `snapcompact` (auto-fallback to context-full) | not in 2026-08-09 (17.3.4 delta) | inherit as working law | [`host-compaction.md`](./host-compaction.md), [`host-delta-17.3.md`](./host-delta-17.3.md) | Does not overturn C. Seal still skips both snapcompact and LLM. |
| HD16 | Plugin cannot set per-session reserve/threshold or teach the UI meter about wire-only savings | not in 2026-08-09 (H1 gap) | inherit as working law | [`host-compaction.md`](./host-compaction.md) | `recordAnchoredHistoryRewrite` is host-internal (shake). Strengthens why overlay-only cannot own headroom. |

---

## 2. Overlay / compress / persist

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| OV1 | Canonical `SessionEntry` journal is lossless. QOL never destructively deletes history | CLOSED | inherit as working law | — | Invariant 5. Native compaction is also append-only (HD7). |
| OV2 | Overlay state is append-only custom session entries / tombstones, not a sidecar DB and not in-place message mutation | CLOSED | inherit as working law | [`host-context-event.md`](./host-context-event.md) | H2 confirmed `appendCustomEntry` → `type: "custom"`, not emitted by `buildSessionContext`. H6: current plugin does **not** call `appendEntry` (deleted with ADR-004 mode emulation). ICM reintroduces it for **different** custom types. |
| OV3 | `pi.appendEntry(customType, data)` persists extension state without sending it to the model | CLOSED | inherit as working law | [`host-context-event.md`](./host-context-event.md) | H2+H5 wiring unchanged. Distinct from `custom_message` / `sendMessage`. Distinct from `sessionManager.appendModeChange`. |
| OV4 | Active overlay is rebuilt by replaying QOL custom entries on the **active branch** | STRONG | inherit as working law | [`host-addressing.md`](./host-addressing.md) | H4: `getBranch()` / `getEntry()` on `ReadonlySessionManager`. That interface does **not** expose `buildSessionContext()`. |
| OV5 | Do not mutate old QOL entries; use tombstones / state transitions | CLOSED | inherit as working law | — | Keeps audit, branch, and rollback deterministic. |
| OV6 | Summary is first-class durable text; raw source is referenced by `(sessionId, entryId)` range, not copied wholesale | CLOSED | inherit as working law | [`host-addressing.md`](./host-addressing.md) | Follows from O6 + OV1. |
| OV7 | Expand before seal = disable the overlay. Exact reverse if the range is still in native active history | STRONG | inherit as working law | [`host-addressing.md`](./host-addressing.md) | After seal see HD12. |
| OV8 | Compression-tool self-footprint is scrubbed **only in projection**: journal keeps the full `summary` argument; next projection replaces it with a bounded marker and sanitizes replay payload | STRONG | inherit as working law | [`dcp-opencode.md`](./dcp-opencode.md), [`pi-dcp.md`](./pi-dcp.md) | D1: OpenCode DCP does not deterministically scrub a successful compress call. H3: structurally cache-friendly; **not** a host feature. |
| OV9 | One `context` handler owns Summary + Pin internal order | STRONG | inherit as working law | [`host-context-event.md`](./host-context-event.md) | H2: viable. `wrapSteeringForModel` still runs **after** all extension handlers. Last *other* extension still wins the array. |
| OV10 | Projection pipeline: native+provenance → replay overlay → valid blocks → protocol closure → summaries → self-footprint scrub → pins → sanitizer → `AgentMessage[]` → host `convertToLlm` | STRONG | inherit as working law | [`host-context-event.md`](./host-context-event.md) | H2 pipeline: `buildSessionContext` → `emitContext` → steering wrap → `convertToLlm` → provider normalize → `transformProviderContext` (obfuscate / snapcompact inline / image clamp). ICM must finish inside `context`, not assume journal bytes reach the provider. |
| OV11 | Active-block summary injects as a bounded synthetic **user-context** record (not system/developer authority) | STRONG | inherit as working law | [`dcp-opencode.md`](./dcp-opencode.md), [`agent-ux.md`](./agent-ux.md) | Markup is a prompt/eval decision. Matches OpenCode DCP + OMP native compaction summaries. |
| OV12 | `context_state` must report raw active / projected / last provider / native pressure / first divergence | STRONG | inherit as working law | [`host-compaction.md`](./host-compaction.md), [`host-cache.md`](./host-cache.md) | “Context is 20k” is ambiguous once QOL and OMP measure different things. |
| OV13 | Overlay event schemas, non-overlap / shadowing, and sealed-block mapping were **not** frozen on 2026-08-09 | OPEN | inherit as working law | [`host-context-event.md`](./host-context-event.md) | Inherit the *need to freeze in design*, not a schema. After H2. |
| OV14 | Project / user / global persistent harness state is a different store from session overlay | CLOSED | out of v1 scope | — | Session overlay is in ICM. Persistent harness is PrimeStyle. |
| OV15 | Current omp-qol plugin has no `context` / `session_before_compact` / `appendEntry` usage | not in 2026-08-09 (H6) | inherit as working law | [`plugin-seams.md`](./plugin-seams.md) | ICM is new engine work on unused hooks. ADR-004 does not block it. |

---

## 3. Provenance / addressing

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| PR1 | `SessionManager` has stable `entry.id` + `parentId` and deterministic branch reconstruction | CLOSED | inherit as working law | [`host-addressing.md`](./host-addressing.md) | H4: 8-hex UUID suffix, collision-checked. Fork copies ids into a new session file. |
| PR2 | `buildSessionContext()` deterministically derives native model messages from the journal | CLOSED | inherit as working law | [`host-addressing.md`](./host-addressing.md) | H4+H5: file unchanged. Projection is **lossy/reordering** vs raw branch (compaction slice, dangling-tool strip, retry elision, fresh objects for summaries). Deterministic ≠ 1:1. |
| PR3 | Public `context` event exposes message copies, not source-entry provenance | DISPROVEN (IDs-on-event) | inherit as working law | [`host-context-event.md`](./host-context-event.md), [`host-addressing.md`](./host-addressing.md) | H2+H4+H5 confirmed. `session_before_compact` *does* expose `branchEntries` + preparation ids — use that for seal, not `context`. |
| PR4 | Context handlers are serial; each later handler sees the previous rewrite | CLOSED | inherit as working law | [`host-context-event.md`](./host-context-event.md) | H2+H5: `emitContext` unchanged. |
| PR5 | Installed plugin is not guaranteed first; `pi.on("context")` has no priority | CLOSED | inherit as working law | [`host-context-event.md`](./host-context-event.md) | H2: discovery is native → hooks → **plugins**. omp-qol as an installed plugin is never first unless also loaded in an earlier stage. |
| PR6 | Persist identity as `(sessionId, entryId)`, never array indexes | CLOSED | inherit as working law | [`host-addressing.md`](./host-addressing.md) | Invariant 7. H4 recommendation 1. |
| PR7 | Short aliases (`@12`, `@m12`, `m0001`) are presentation only. Do not freeze public syntax until provenance survives branch/resume/compaction/hidden/multi-tool/retry/other transformers | OPEN / DEFER | inherit as working law | [`host-addressing.md`](./host-addressing.md), [`agent-ux.md`](./agent-ux.md) | Invariant tension + Q2. H4: plugin-only alias is overlay-local persistence at best; `stable: false` if shown. |
| PR8 | A small host/core provenance seam (`ContextRecord` or sidecar) is the correct long-term identity substrate | STRONG recommendation | inherit as working law | [`host-addressing.md`](./host-addressing.md), [`host-delta-17.3.md`](./host-delta-17.3.md) | H5: **not added** in 17.3.4. Still the recommended seam. v1 may ship a hybrid (PR9) without it. |
| PR9 | Hybrid option: alias for the model, `(sessionId, entryId)` for persistence, provenance seam when obtainable | OPEN (Q2) | inherit as working law | [`host-addressing.md`](./host-addressing.md) | Inherit as an *option*, not a freeze. H4 leans: project from `getBranch()` + entry ids; treat `context` messages as malleable output. |
| PR10 | Adversarial provenance cases: branch/tree, resume, native compaction, branch summary, hidden/custom, multi-tool, retry/error, other context extensions before/after QOL | OPEN | must re-verify on 17.3.4 | [`host-addressing.md`](./host-addressing.md) | H4 listed the breakages. E3 fixtures still required before any public syntax. |

---

## 4. Protocol safety

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| PT1 | Semantic range selection is not enough. Normalize to a protocol-safe closure before projection | DISPROVEN (blind delete) | inherit as working law | [`dcp-opencode.md`](./dcp-opencode.md), [`pi-dcp.md`](./pi-dcp.md) | Invariant 8. Report requested vs normalized range. |
| PT2 | No orphaned raw `toolResult`; no retained provider tool call that expects a removed result; expand or reject bad boundaries | CLOSED | inherit as working law | [`dcp-opencode.md`](./dcp-opencode.md) | Steal DCP pairing discipline; do not steal auto-prune as product. |
| PT3 | Multi-tool assistant: keep unrelated sibling calls; reconstruct/sanitize the projected assistant; strip replay-bound provider payload after content surgery | CLOSED | inherit as working law | [`host-context-event.md`](./host-context-event.md), [`host-delta-17.3.md`](./host-delta-17.3.md) | H5: Cursor `exec-resolved` symbol must survive snapshots. Do not strip unknown provider metadata casually. |
| PT4 | Pinned tool results render as provider-neutral text, not a raw standalone `toolResult` | DISPROVEN (replay) | inherit as working law | [`opencode-acm.md`](./opencode-acm.md), [`pin-ecosystem.md`](./pin-ecosystem.md) | Invariant 9. Decouples salience from protocol pairing. |
| PT5 | Projected assistant replay sanitization is required for Anthropic-like and OpenAI Responses-like families | STRONG / OPEN | must re-verify on 17.3.4 | [`host-cache.md`](./host-cache.md), [`host-context-event.md`](./host-context-event.md) | H3: digest ≠ wire bytes; `transformProviderContext` can shift cache without changing digests. L5 provider-wire still required. |

---

## 5. Cache

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| CA1 | `AppendOnlyContextManager` preserves the longest byte-stable provider-message prefix after an in-place rewrite | CLOSED | inherit as working law | [`host-cache.md`](./host-cache.md) | H3+H5: file unchanged since 17.2.12. Mechanism since 16.1.18 (#3406). |
| CA2 | Cache cost is **first divergence frontier + changed suffix**, not “rewrote history = lost all cache” | CLOSED | inherit as working law | [`host-cache.md`](./host-cache.md) | Invariant 11. H3 confirmed for in-place rewrite. **Exception:** compaction shrink full-clears the append-only log. |
| CA3 | Tail-zone pins are usually cache-friendly | STRONG hypothesis | inherit as working law | [`host-cache.md`](./host-cache.md), [`cache-cost.md`](./cache-cost.md) | Inherit as v1 **default**. H3 confirmed structurally. Economics still need E1 on current providers. Q7. |
| CA4 | Scrubbing the compression tool’s own large `summary` argument in the recent tail is cache-friendly | STRONG | inherit as working law | [`host-cache.md`](./host-cache.md), [`dcp-opencode.md`](./dcp-opencode.md) | Structurally yes; host does not implement the scrub (OV8). |
| CA5 | Deep old-range compression invalidates the suffix from the compression anchor, not necessarily the whole prompt | STRONG | inherit as working law | [`host-cache.md`](./host-cache.md) | H3 confirmed for in-place rewrite path. |
| CA6 | System-prompt / system-placement pins can invalidate the stable system+tool prefix and should be rare and explicit | STRONG | inherit as working law | [`host-cache.md`](./host-cache.md) | H3: `StablePrefix` fingerprint. Live `refreshBaseSystemPrompt` is a prefix-churn event (adjacent). |
| CA7 | Arbitrary mid-history pin stays experimental until measured benefit exceeds cache cost | DEFER | inherit as working law | [`cache-cost.md`](./cache-cost.md), [`pin-ecosystem.md`](./pin-ecosystem.md) | Not v1 default. |
| CA8 | Measure `cacheRead` / `cacheWrite` / input / output / native pressure / first divergence across arms: native, overlay, overlay+seal, overlay+pin | OPEN (Q8) | must re-verify on 17.3.4 | [`cache-cost.md`](./cache-cost.md) | H3: no exported divergence API; append-only `auto` is mainly DeepSeek/local; Anthropic often off unless forced. H5 adds `anthropicCacheRefresh: true`, advisor delta-split, image clamp→normalize. E1 must include those variables. |
| CA9 | Expected ranking: tail-zone pin ≪ mid-history pin ≪/varies system-prefix churn | OPEN / provider-dependent | must re-verify on 17.3.4 | [`cache-cost.md`](./cache-cost.md) | Hypothesis to measure, not a provider-universal law. |

---

## 6. Pin

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| PN1 | A pin is a **salience intent**, not a copied message and not inherently a system-prompt mutation | CLOSED | inherit as working law | [`opencode-acm.md`](./opencode-acm.md), [`pin-ecosystem.md`](./pin-ecosystem.md) | Invariant 9. |
| PN2 | Three kinds: `source` / `snapshot` / `instruction` | OPEN (make kind explicit) | inherit as working law | [`pin-ecosystem.md`](./pin-ecosystem.md) | Working design, not a frozen schema. |
| PN3 | v1 default scope is **branch** (custom entries on the active `SessionEntry` branch) | OPEN (other scopes valid) | inherit as working law | [`host-addressing.md`](./host-addressing.md) | Create on A → descendants inherit; sibling from before the pin → absent; remove → tombstone. |
| PN4 | Later scopes (session-wide, tree-derived, project/user via Managed Harness Resources) must not be accidental side effects of a session pin | DEFER | out of v1 scope | — | Tree-derived is Phase 6 (in program). Project/user MHR is PrimeStyle. |
| PN5 | Tail-zone = inject the combined pin block immediately **before the current turn’s request frontier**, not blindly after the latest user line | STRONG | inherit as working law | [`host-cache.md`](./host-cache.md), [`agent-ux.md`](./agent-ux.md) | Validate against steering / follow-up / tool-loop. H5 `/shake` 4k tail protection is a host rhyme, not a pin API. |
| PN6 | Only instruction-class pins are default-eligible for system placement | STRONG | inherit as working law | [`pin-ecosystem.md`](./pin-ecosystem.md) | Standing constraints, not ordinary facts. Cache cost is CA6. |
| PN7 | Prefer one bounded synthetic pin block in deterministic order over N extra provider messages | STRONG | inherit as working law | [`opencode-acm.md`](./opencode-acm.md) | Order sketch: instruction by priority, then source/snapshot by id, then tree-derived with path provenance. |
| PN8 | A source pin may reference content inside an active compressed block; inspection shows coverage (`pin p3` covered by `b17`) | STRONG | inherit as working law | — | Compression removes the raw region; the pin still renders the salient slice. |
| PN9 | Pin compaction modes: `request-only` / `salient` / `preserve` | STRONG | inherit as working law | [`host-compaction.md`](./host-compaction.md) | Ordinary native: `session.compacting`. Custom seal: merge required pins into summary / `preserveData`. Names can change. |
| PN10 | After a native seal, a source pin still renders from the canonical journal, not from ordinary `buildSessionContext()` output | STRONG | inherit as working law | [`host-compaction.md`](./host-compaction.md), [`host-addressing.md`](./host-addressing.md) | Depends on OV1 + HD7. |
| PN11 | Pin implementation must be complete without any tree. PinStateTree must not own storage, rendering, compaction, addressing, or protocol safety | CLOSED | inherit as working law | — | Invariant 10. |
| PN12 | Token savings alone cannot validate Pin. Eval: constraint retention, factual recall, no authority escalation, post-compaction survival, unpin when asked | STRONG | inherit as working law | [`cache-cost.md`](./cache-cost.md) | Behavior + cache/cost together. |
| PN13 | System prompt is **not** universally the best pin location | OPEN | must re-verify on 17.3.4 | [`cache-cost.md`](./cache-cost.md), [`pin-ecosystem.md`](./pin-ecosystem.md) | Authority vs cache vs recency. v1 default remains tail (PN5). |

---

## 7. PinStateTree

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| TR1 | PinStateTree is a policy/state machine that selects pin intents. It depends on Pin; Pin does not depend on it | DEFER / rejected as core | inherit as working law | [`pin-ecosystem.md`](./pin-ecosystem.md) | In program as Phase 6, not pin v1. |
| TR2 | Exactly one active leaf per tree; many trees may be active; display current path + siblings only | pillar + STRONG | inherit as working law | — | Pillar 4. Do not dump the full graph unless asked. |
| TR3 | `march(child)` and `jump(node)` are atomic: update controller, recompute derived pins, report new path/pins before the next model request | STRONG | inherit as working law | [`agent-ux.md`](./agent-ux.md) | |
| TR4 | Do not auto-resolve contradictory instruction text. Dedup identical source/snapshot by identity/hash. Inspection shows manual vs tree+node provenance | STRONG | inherit as working law | — | Later optional tree priority may order, never silently delete a conflicting instruction. |
| TR5 | Split **definition** persistence (reusable workflow/profile) from **runtime** active-leaf persistence (session custom entry) | STRONG | inherit as working law | — | |
| TR6 | Defer implementation until flat pin has E3/E4 evidence (address, branch/resume, tail/system, compaction survival, conflict inspect, model can use plain pin) | DEFER | inherit as working law | [`pin-ecosystem.md`](./pin-ecosystem.md), [`opencode-acm.md`](./opencode-acm.md) | Promote only if it beats manual pin on missed phase constraints or CM overhead. |

---

## 8. Agent UX / auto-policy (architecture-adjacent)

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| UX1 | Plugin-decided automatic “this should be compressed” is not the product. Heuristic skills/instructions are allowed | pillar | inherit as working law | [`dcp-opencode.md`](./dcp-opencode.md) | Invariant 2. D1: steal overlay/block/pairing; reject or demote auto-dedup / error-purge / nudges to opt-off. |
| UX2 | How much automatic policy is allowed (off-by-default inspectable vs never-auto) is still open | OPEN (Q5) | inherit as working law | [`dcp-opencode.md`](./dcp-opencode.md), [`agent-ux.md`](./agent-ux.md) | Inherit the *tension*. Do not paper over. Do not freeze a hidden auto engine. |
| UX3 | Agent-facing tool shape (one `context` multi-op vs separate `compress` / `pin` / `tree`) is open | OPEN (Q6) | inherit as working law | [`agent-ux.md`](./agent-ux.md), [`plugin-seams.md`](./plugin-seams.md) | Inherit as open. Existing QOL pattern is one tool per domain, structured JSON, dynamic read/write approval. |
| UX4 | ADR-004 forbids emulating host features that exist; ICM features do not exist, so an overlay engine is new work | tension | inherit as working law | [`plugin-seams.md`](./plugin-seams.md) | H6 closed the framing. Needs a sibling ICM ADR, not a silent rewrite of ADR-004. |
| UX5 | Expose the host primitive; if inaccessible, add the smallest bridge; do not emulate an OMP subsystem in the plugin | CLOSED (Layer 1 law) | inherit as working law | [`plugin-seams.md`](./plugin-seams.md) | Applies to Goal/Plan/Advisor. For ICM the host gives **hooks**, not a product. |

---

## 9. Adjacent / out of this program (Advisor, PrimeStyle, Goal, Plan, Vibe)

Not ICM v1. Listed so a later agent does not treat 2026-08-09 P0 “advisor bridge” / “base-prompt refresh” as this program’s blockers.

| ID | Claim | 2026-08-09 status | Disposition | Closer | Notes |
|---|---|---|---|---|---|
| AD1 | Advisor roster hot-apply is already native (`save` → discover → `applyAdvisorConfigs`) | CLOSED | out of v1 scope | [`plugin-seams.md`](./plugin-seams.md) | QOL-004 already implements this. 2026-08-09 P0 “advisor host bridge” is closed for the plugin, not an ICM task. |
| AD2 | `/advisor off/on` is enable/disable only, not rediscovery | CLOSED | out of v1 scope | [`plugin-seams.md`](./plugin-seams.md) | |
| AD3 | `ctx.reload()` reopens/switches the session file | DISPROVEN (as refresh) | out of v1 scope | [`plugin-seams.md`](./plugin-seams.md) | See O13. |
| AD4 | Task-agent definitions rediscover each spawn; model roles resolve from live Settings | DISPROVEN (file-edit-is-enough) | out of v1 scope | — | PrimeStyle / mode-adjacent. |
| AD5 | `manage_skill` + `refreshSkills` exist; reuse, do not duplicate | DISPROVEN (no native skill mgmt) | out of v1 scope | — | PrimeStyle. |
| AD6 | MCP has its own reconnect / rediscover / rebind lifecycle | DISPROVEN (generic reload) | out of v1 scope | — | |
| AD7 | Model catalog has native refresh | DISPROVEN (no refresh) | out of v1 scope | — | |
| AD8 | AGENTS/context can live-rebuild via `refreshBaseSystemPrompt()` when not frozen; SYSTEM/APPEND are startup-captured strings | DISPROVEN (same live-apply) | out of v1 scope | [`host-delta-17.3.md`](./host-delta-17.3.md) | STATUS #7. Not in H5 required diff. Prefix-churn if ICM ever pins into system (CA6). |
| AD9 | Running extension source is restart-class in v1; no supported teardown / deregister / replace protocol | CLOSED conservatively | out of v1 scope | [`plugin-seams.md`](./plugin-seams.md) | H5 late tool registration is a **new** integration seam, not live self-edit. |
| AD10 | Prime-style management is typed resources + credit/activation/eval, not unrestricted file edit | DISPROVEN (file-edit unlock) | out of v1 scope | — | 2026-08-16 author scope. |
| AD11 | Goal and Plan/Vibe are substantially solved; ADR-004 remains the law | CLOSED | out of v1 scope | [`plugin-seams.md`](./plugin-seams.md) | Maintenance only. |
| AD12 | Resource apply result schema (`persisted` / `applied` / `effectiveAt` / `verification` / `warnings`) | OPEN P0 in 2026-08-09 | out of v1 scope | — | PrimeStyle. ICM tools should still copy the QOL JSON envelope (H6). |
| AD13 | `packages/metaharness` exists as an upstream measurement direction | STRONG (adjacent) | out of v1 scope | [`cache-cost.md`](./cache-cost.md) | May inform E1 later. Not a v1 deliverable. |
| AD14 | Host-native `omp compress` is semantic **file** compression, not session overlay | 17.3.4 new seam | out of v1 scope | [`host-delta-17.3.md`](./host-delta-17.3.md) | Differentiate initiative session overlay from file CLI and from native snapcompact / `/shake`. |

---

## Cross-walk to open questions

| Question | Ingest rows | 2026-08-16 evidence |
|---|---|---|
| **Q1** Still floor by stored conversation? | O2, HD1–HD3 | **Yes** (H1 E2). Overlay-only cannot own headroom. C or D still required. |
| **Q2** Plugin alias first, or host provenance first? | PR3–PR10 | Gap **unchanged** (H2/H4/H5). Hybrid still the practical v1 option. Do not freeze `@N`. |
| **Q3** Product target C or D? | HD8–HD13 | **Not decided.** C still viable; D not shipped; pillar leans D. |
| **Q4** Expand after native seal? | HD12, OV7 | Distinction inherited. UX not frozen. |
| **Q5** How much automatic policy? | UX1–UX2 | Tension inherited. D1: auto-policy is DCP’s value and the pillar’s reject. |
| **Q6** Agent-facing tool shape? | UX3 | Open. H6: one tool per domain + JSON envelope is the existing QOL pattern. |
| **Q7** Pin default placement? | PN5–PN6, PN13, CA3, CA6–CA9 | Tail default inherited. Measurement still E1. |
| **Q8** How to measure cache and price? | CA8–CA9, OV12 | Fixture still required. H3 documents host fields and the missing divergence export. |

---

## Counts

| Disposition | ICM core (O ICM + HD–UX) | Adjacent (AD + O13–O16) |
|---|---|---|
| inherit as working law | majority of replacement findings + design laws; host facts H1–H5 confirmed | — |
| must re-verify on 17.3.4 | PR10 adversarial fixtures; PT5 provider-wire; CA8–CA9 / PN13 economics | — |
| already overturned | O1–O12 (original false assumptions) | O13–O16 classified out of v1 |
| out of v1 scope | OV14 project/user harness; PN4 MHR scopes | AD1–AD14 |

No 2026-08-09 **ICM host finding** was overturned on 17.3.4. What changed is orchestration, default snapcompact, last-handler-wins, `anthropicCacheRefresh`, late tool registration, and new adjacent compress products.

---

## Sources (read-only)

Handoff (do not edit):

- `docs/researches/OMP-QOL-Complete-Research-Handoff-2026-08-09/01-FOUNDATION-REPORT.md`
- `.../02-foundation-research/03-UNCERTAINTY-REGISTER.md`
- `.../02-foundation-research/14-P0-CLOSURE-NOTES.md`
- `.../02-foundation-research/07-CONTEXT-OVERLAY-ENGINE.md`
- `.../02-foundation-research/08-PIN.md` and `09-PIN-STATE-TREE.md` (pin/tree completeness)
- `.../02-foundation-research/04-UPSTREAM-DELTA.md` (lifecycle deltas)

Workspace (read):

- [`../INVARIANTS.md`](../INVARIANTS.md)
- [`../questions/open-questions.md`](../questions/open-questions.md)
- [`plugin-seams.md`](./plugin-seams.md) (H6)
- [`host-compaction.md`](./host-compaction.md) (H1)
- [`host-context-event.md`](./host-context-event.md) (H2)
- [`host-cache.md`](./host-cache.md) (H3)
- [`host-addressing.md`](./host-addressing.md) (H4)
- [`host-delta-17.3.md`](./host-delta-17.3.md) (H5)
