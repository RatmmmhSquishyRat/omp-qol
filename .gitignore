# dependencies
node_modules/

# generated delivery artifacts — regenerate with `bun .sandbox/install-plugin.ts`
# (contains machine-absolute installPath and a Windows junction; never commit)
test-workspace/.omp/

# sandbox runtime noise
.sandbox/scratch/
*.log

# CI / local vendor checkout of the host (never commit)
.vendor/

# editor / OS noise
.DS_Store
Thumbs.db
