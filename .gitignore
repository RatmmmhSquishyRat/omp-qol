# dependencies
node_modules/

# leftover project-local plugin tree from the retired copy/junction installer.
# Do not refresh this while a live omp session is reading it. After sessions
# end, official install is `omp plugin install omp-qol-plugin` (user root).
test-workspace/.omp/

# sandbox runtime noise
.sandbox/scratch/
*.log

# CI / local vendor checkout of the host (never commit)
.vendor/

# editor / OS noise
.DS_Store
Thumbs.db
